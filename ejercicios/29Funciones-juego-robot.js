// Define funciones para movimientos reutilizables (unidad funciones)
// Ejemplo:

function avanzarDerecha(pasos) {
    robot.repeat(pasos,() => robot.moveRight());
}

avanzarDerecha(6);


function avanzarAbajo (pasos) {
    robot.repeat(pasos,() => robot.moveDown());
}

avanzarAbajo(2);


function avanzarIzquierda (pasos) {
    robot.repeat(pasos,() => robot.moveLeft());
}

avanzarIzquierda(6);

function avanzarAbajo (pasos) {
    robot.repeat(pasos,() => robot.moveDown());
}

avanzarAbajo(2);

function avanzarDerecha(pasos) {
    robot.repeat(pasos,() => robot.moveRight());
}

avanzarDerecha(6);

function avanzarAbajo (pasos) {
    robot.repeat(pasos,() => robot.moveDown());
}

avanzarAbajo(1);
